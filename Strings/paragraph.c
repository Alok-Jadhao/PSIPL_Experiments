#include <stdio.h>
#include <string.h>

//check spaces, new line , characters and words.

int main()
{
    char str[100];
    scanf("%[^~]",str);

    int length = strlen(str)

    for(int i=0;i!="\n";i++)
    {
        printf("The number of lines are: ")
    }

    return 0;
}